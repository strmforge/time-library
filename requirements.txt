# Time Library Python 依赖
# ============================================================
# 注意：本项目使用虚拟环境（venv/），运行时通过 venv/bin/python3 执行
# 所有依赖通过 pip install -r requirements-*.txt 安装
# ============================================================

# 安装所有依赖：
#   pip install -r requirements-vector.txt

# 查看已安装的包：
#   pip list

# requirements 拆分说明：
#   requirements-core.txt   - 核心运行时（标准库 + 最少依赖）
#   requirements-vector.txt - 向量召回依赖（LanceDB / Granite / torch / transformers）
#   requirements-dev.txt   - 开发/调试依赖（可选）
