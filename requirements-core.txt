# Time Library core runtime dependencies
# Install with: pip install -r requirements-core.txt
#
# The following are Python standard-library modules and need no installation:
#   os, sys, json, glob, argparse, shutil, time, signal, re, datetime
#   pathlib, collections, threading, http.server, urllib.parse
#
# Additional runtime dependencies:
watchdog>=4.0.0
inotify>=0.2.12; sys_platform == 'linux'
cryptography>=41.0.0
PyYAML>=6.0.0
